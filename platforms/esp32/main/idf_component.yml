## SPDX-License-Identifier: Apache-2.0
dependencies:
  idf: "==6.1"
  espressif/usb:
    version: "1.5.0"
    rules:
      - if: "target in [esp32s3]"
  espressif/usb_host_hid:
    version: "1.2.0"
    rules:
      - if: "target in [esp32s3]"
