# Platform Matrix

Statuses follow `CODEX_GOAL.md`. A platform is never promoted without a real
toolchain result recorded here.

| Platform | Status | Verified | Toolchain / device | Command and evidence |
|---|---|---|---|---|
| Windows x64 | runtime-verified (wxWebView instrument, native debugger, and headless service) | 2026-09-06 | VS 2026 18.8.0; MSVC 19.51.36248; wxWidgets 3.2.11; WebView2; miniaudio 0.11.25; WASAPI; Raw Input; WinMM MIDI | Real `mol-keyboard.exe` and `mol-keyboard-debug.exe` windows were opened and inspected. Candidate `95c6cee` passes the hosted native suite and its exact package audit produced a 4,468,339-byte, 154-file ZIP (`09439f529367ac639e747d16a44561473149c92f9b1ed70f1fd500c3a56da49b`). Its extracted WebView2 instrument reports `PASS-SharedArrayBuffer`; its native debugger controls and shuts down an independent packaged daemon through Named Pipe state, capability, self-test, note, release, and all-sound-off calls; a fresh packaged daemon then passes the complete no-UI CLI lifecycle. The prior isolated install-prefix, Startup shortcut, authenticated browser-to-service, real WASAPI, and Raw Input evidence remains valid. The host exposed no physical MIDI or Bluetooth output; those remain unverified |
| Windows arm64 | runtime-verified (native headless suite) | 2026-09-06 | GitHub-hosted Windows 11 ARM64; VS 2026; native ARM64 host/toolchain | Candidate `95c6cee` passes 99/99 tests on the native `windows-11-vs2026-arm` runner, including the daemon process, CLI, Startup-service model, rendering, and platform contracts. The earlier LLVM-MinGW cross-build remains a separate object-format/build gate. The hosted VM exposes no physical GUI, audio, input, MIDI, Bluetooth, scheduling, or latency evidence |
| Linux x86_64 | runtime-verified (wxWebView/native debugger under Xvfb, headless service) | 2026-09-06 | GitHub-hosted Ubuntu 24.04; Xvfb; GTK/WebKitGTK; wxWidgets 3.2.11 | Candidate `95c6cee` passes the full GUI-enabled Release+LTO suite 103/103 and produces a 7,577,027-byte, 153-file TGZ (`1a1807ecf98660aaabacc8cee665c4278fb50c8e50acdaf2ea776831a40f12fe`). Its extracted GTK/WebKitGTK instrument reports `PASS-MessagePort` under Xvfb; its native debugger controls and shuts down an independent packaged daemon over a Unix socket; a fresh packaged daemon then passes the complete no-UI CLI lifecycle. The production raw-MIDI kernel-FIFO simulation, real systemd user manager, installed consumers, ABI, authenticated browser UI, and controlled launchd model also pass. Xvfb is not physical display evidence; physical evdev/MIDI and native audio hardware are not exposed |
| Linux aarch64 | runtime-verified (native headless suite); simulated-runtime (QEMU) | 2026-09-06 | GitHub-hosted Ubuntu 24.04 ARM64, GNU 13.3.0; GNU 15.2.0/QEMU 10.2.1 cross gate | Candidate `95c6cee` passes 101/101 tests on the native `ubuntu-24.04-arm` runner, including the daemon, CLI, renderer, and process lifecycle. The independent QEMU gate still exercises the AArch64 Release product and 71/71 target tests. Neither hosted nor QEMU execution exposes physical audio, evdev, MIDI, scheduling, latency, or device-lifecycle evidence |
| macOS arm64/x64 | runtime-verified (wxWebView instrument, native debugger, and headless service) | 2026-09-06 | GitHub-hosted macOS 15 arm64 and Intel x86_64; AppleClang 17.0.0.17000013; wxWidgets 3.2.11; WKWebView | Candidate `95c6cee` passes 96/96 native tests on both architectures, including the real temporary LaunchAgent daemon/CLI lifecycle. Each Release+LTO desktop job passes 4/4 focused WKWebView/native-debugger tests and audits the extracted GUI, debugger-to-independent-daemon IPC, and separate no-UI null-audio lifecycle. The 167-file arm64 archive is 3,937,186 bytes (`55a973bfc2d2cacd9f74d39d0388c48474d43f8fd01113286eac7b1ed9f6799b`); x86_64 is 4,062,987 bytes (`21c5a6b8d7cd8dde48e40cfedca76e1081c32fdb2c07ee7c53f35e8be9c8495b`). Controlled IOHID/CoreAudio/CoreMIDI fault models also pass. Physical permissions, devices, routes, suspend/resume, Bluetooth, and latency remain unverified |
| WebAssembly | runtime-verified (M6 PWA) | 2026-09-06 | Emscripten 6.0.5; Node.js 22; Chrome 151; Edge 152; Firefox 153; Playwright WebKit 26.5; actual Safari 26.6.1 on hosted macOS 15 arm64 | The production bundle passes 12/12 unit tests and the complete Linux browser/real-daemon CI matrix. Browser automation covers Chrome/Edge/Chromium-mobile realtime AudioWorklet, Firefox offline AudioWorklet/Wasm DSP, Chrome offline startup/play, busy-main-thread stability, blur release, lifecycle resume, SAB, MessagePort, desktop service control, and mobile layouts. Candidate `f8081ca` adds an independent SafariDriver gate against the system Safari—not Playwright WebKit—and requires the complete 30-key UI, manifest, IndexedDB/service-worker capabilities, native click-to-start, realtime Wasm/AudioWorklet readiness, MessagePort fallback, a keyboard event reaching the engine, zero drops, and clean note release. Unified candidate `95c6cee` reports `MOL_SAFARI_SMOKE_PASS 26.6.1 events=3 transport=MessagePort baseline` in fully green run 15. Physical browser audio routes and latency remain unverified |
| Android arm64/x86_64 | runtime-verified (x86_64 emulator); arm64 build-verified | 2026-09-06 | JDK 21; Gradle 8.11.1; AGP 8.10.1; Kotlin 2.1.20; API 36; NDK 28.2.13676358; CMake 3.31.6; Oboe 1.10.0; Android 15/API 35 emulator | Candidate `95c6cee` passes Debug/Release/test dual-ABI builds, lint, 46/46 Wasm tests, and 12/12 Web tests. Its fail-closed headless-emulator gate verifies the packaged UI-to-AAudio chain at 48 kHz, all 30 production native-key mappings and repeat suppression, injected focus recovery, foreground/background/screen-off/idle lifecycle, and service cleanup. Physical device delivery, real competing-app focus, routes, long-run behavior, and latency remain unverified |
| iOS arm64 | build-verified; runtime-verified (iPhone Simulator UI/bridge) | 2026-09-06 | Xcode 16.4; iOS 18.5 SDK; AppleClang 17; x86_64/arm64 Simulator and arm64 Device builds | Candidate `95c6cee` builds the complete UIKit/WKWebView, RemoteIO/AVAudioSession application with warnings as errors for both Simulator architectures and unsigned arm64 Device. The gate validates the flat app bundle, property lists, privacy manifest, assets, localization, and Web resources, then installs and launches the app in an iPhone Simulator and receives `MOL_IOS_SIMULATOR_SMOKE_PASS` after valid and rejected production reply-bridge requests. Portable lifecycle/key ownership and sanitizer tests also pass. Physical audio, lifecycle, routes, hardware keyboard, signing/install, endurance, and latency remain unverified |
| HarmonyOS arm64/x86_64 | formal product implementation-complete/source-checked; policy/native boundary simulated; OpenHarmony compatibility build-verified | 2026-09-03 | OpenHarmony 5.0.0.71/API 12; Hvigor/OHOS plugin 5.8.9; JDK 21.0.12.1; Node.js 22.16.0; MSVC 19.51, GNU 15.2.0, Emscripten 6.0.5, Clang ASan/UBSan, and AArch64 QEMU simulation; no DevEco/device | Clean Debug and Release compatibility HAPs contain ArkTS bytecode plus AArch64 and x86-64 OHAudio libraries; Release is unsigned and 2,953,954 bytes. The recorded clean-run archive SHA-256 is `3AE2368D3A2F1901390B6C56164A26DE8A0AC07700DD512F84FBEE6664FAE312`; stable packaged-bytecode SHA-256 is `C5DB99A0968F9514BC37BEEB6731E1E4BCF12BB3E14C640BB647C945077C545B`, while the raw archive includes timestamps. The exact production `.ets` policy source executes without transformation under Node and compiles into both HAP variants. The unchanged production Node-API bridge and OHAudio host execute registration, strict validation, status/event/recording transfer, fast/fallback PCM, route/interruption/error recovery, and failure injection across x64, Wasm, sanitizers, and AArch64 QEMU. AudioSession, AVSession, continuous-task delivery, HarmonyOS scheduling, signing, and device acceptance remain unverified |
| ESP32 | build-verified; emulated-firmware (M9 implementation) | 2026-09-05 | ESP-IDF 6.1; GNU 15.2.0; Espressif QEMU 9.2.2; no board attached | Clean exact-source candidate `19735a9` builds 1,018,320-byte default and 1,551,232-byte Web images; isolated firmware boots in QEMU and passes storage, shared sequence/C4, 12 production commands, and 96,000 FreeRTOS audio frames with a virtual sink; 30-minute virtual telemetry passes; GPIO, BLE/Classic HID, I2S, A2DP Source/AVRCP, NVS/FAT, recovery gestures, control task, and physical AP-only configuration are implemented; physical radio/audio/real-time/long-run acceptance is unverified |
| ESP32-S3 | build-verified; emulated-firmware (M9 implementation) | 2026-09-05 | ESP-IDF 6.1; GNU 15.2.0; Espressif QEMU 9.2.2; no board attached | Clean exact-source candidate `19735a9` builds 796,896-byte default and 1,302,256-byte Web images; isolated firmware boots in QEMU and passes storage, shared sequence/C4, 12 production commands, and 98,176 FreeRTOS audio frames with a virtual sink; 30-minute virtual telemetry passes; GPIO, BLE and USB HID, I2S, NVS/FAT, recovery gestures, control task, and physical AP-only configuration are implemented; Classic A2DP is explicitly absent; physical input/audio/real-time/long-run acceptance is unverified |

## Capability boundaries

- Desktop and mobile Bluetooth audio is routed by the operating system.
- Bluetooth latency does not use the wired low-latency acceptance threshold.
- Mobile hardware keyboard input is promised only while the application can
  legally receive it.
- Browser keyboard input requires focus and browser audio requires a user gesture.
- ESP32-S3 does not advertise Classic Bluetooth A2DP Source capability.
