# Platform Matrix

Statuses follow `CODEX_GOAL.md`. A platform is never promoted without a real
toolchain result recorded here.

| Platform | Status | Verified | Toolchain / device | Command and evidence |
|---|---|---|---|---|
| Windows x64 | runtime-verified (M5 app and headless service) | 2026-09-04 | VS 2026 18.8.0; MSVC 19.51.36248; miniaudio 0.11.25; WASAPI; Raw Input; WinMM MIDI; WScript Startup shortcut; system Chrome 151.0.7922.175 | Current LTO Release passes 93/93, including WinMM API enumeration and bounded MIDI stream semantics; the host exposed no physical MIDI endpoint. A fresh 1,328,430-byte optimized ZIP (`a6d83dee02dcdb6c10dd774d84d587fc53611d9e0020bf39fb2f1f4f0f088237`) was safely extracted and its packaged daemon/CLI passed the complete no-UI null-audio control, recording/playback, diagnostics, finite benchmark, MIDI-capability, and clean-shutdown lifecycle. Exact-candidate clean clone `23ff832` compiled all 176 targets and passed 93/93. The isolated install-prefix gate compiles all 14 public headers as C11/C++17 and runs both installed-package consumers. A real temporary Startup `.lnk` launched the exact headless daemon and was removed by the production uninstaller after full CLI control and exit-code-zero shutdown. The current locked production Web UI passed all five applicable Chrome desktop cases while controlling an independent daemon over authenticated loopback WebSocket and Named Pipe. Real 48 kHz audio and Raw Input keyboard evidence exists; physical MIDI and Bluetooth output remain unverified/unavailable |
| Windows arm64 | build-verified | 2026-09-03 | LLVM-MinGW 20260826 UCRT; Clang 23.1.0; x64 cross host | Checked-in preset produced COFF-ARM64 `mol_core`, daemon, CLI, playback, sequence, render, patch, and analyzer outputs; a native Windows ARM64 CI runner is configured, but runtime/audio/input remain unverified until that job or a device run completes |
| Linux x86_64 | runtime-verified (M5 app and service, WSL/null audio) | 2026-09-04 | WSL; systemd 259 user manager; GNU 15.2.0; Node.js 22.16.0; Linux raw-MIDI; bundled Chrome for Testing 151.0.7922.34 | Current GCC suite passes 95/95. The production raw-MIDI adapter passes enumeration, open, background poll/read, channel filtering, commands, and disconnect cleanup against a kernel FIFO; this is simulated MIDI rather than hardware evidence. A fresh 1,714,599-byte optimized TGZ (`648105f02ce5765859c7e209a50e6718458f34b6c347624403a76d45af9fd6fc`) was safely extracted and its packaged daemon/CLI passed the complete no-UI null-audio control, recording/playback, diagnostics, finite benchmark, MIDI-capability, clean-shutdown, and socket-cleanup lifecycle. Exact-candidate clean clone `23ff832` compiled all 180 targets and passed 95/95. The real systemd user manager passes the shipped sandbox policy and service lifecycle; installed C/C++ consumers, the 47-symbol ABI, authenticated production Web UI, and controlled launchd model also pass. Physical evdev/MIDI and native audio hardware are not exposed |
| Linux aarch64 | build-verified; simulated-runtime (QEMU) | 2026-09-03 | Ubuntu resolute cross sysroot; GNU 15.2.0; QEMU 10.2.1 on WSL x86_64 | Release AArch64 ELF daemon, CLI, and renderer completed IPC, record/playback, doctor, self-test, benchmark, shutdown, and finite WAV checks; Debug target binaries passed 71/71 tests including golden audio metrics, mobile policy/native-boundary simulations, and nested processes. QEMU is not native ARM64, physical audio, evdev, scheduling, latency, or device-lifecycle evidence |
| macOS arm64/x64 | source-reviewed; simulated IOHID/CoreAudio/CoreMIDI/LaunchAgent lifecycle | 2026-09-04 | Apple SDK unavailable; production platform sources executed against controlled API models under MSVC and Linux GCC | IOHID enumeration/gesture cleanup, CoreAudio callback/device recovery, and CoreMIDI endpoint enumeration, channel filtering, callback translation, and disposal simulations pass. The unchanged Apple service runner also passes on Linux against a controlled launchd model while driving the real daemon/CLI through null-audio startup, control, diagnostics, shutdown, and cleanup. Apple CI has the native fail-closed CTest, but it has not executed on this host. Apple framework compilation, native launchd behavior, permissions, physical audio/keyboard/MIDI, and native runtime remain unverified |
| WebAssembly | runtime-verified (M6 PWA; Safari pending) | 2026-09-04 | Emscripten 6.0.5; Node.js 22.16.0; Chrome 151.0.7922.175; Edge 152.0.4191.53; Firefox 153.0; Chromium 151.0.7922.34; WebKit 26.5 | Exact-candidate clean clone `23ff832` compiled all 108 targets and passed MinSizeRel 42/42; prior Debug passed 31/31. Browser matrix 15 applicable passes and 27 explicit project skips across 42 combinations; Chrome/Edge/Chromium-mobile realtime AudioWorklet, Firefox offline AudioWorklet/Wasm DSP, Chrome offline audio startup/play, busy-main-thread stability, blur release, lifecycle resume, SAB, and MessagePort paths pass. The checked-in Linux CI path now builds and requires the real daemon before the full browser matrix; its portable audit passes locally, while the workflow remains unexecuted for the unpushed commit. WebKit UI/layout passes but Windows WebKit lacks AudioWorklet and is not claimed as Safari |
| Android arm64/x86_64 | runtime-verified (x86_64 emulator); arm64 build-verified | 2026-09-03 | JDK 21.0.12.1; Gradle 8.11.1; AGP 8.10.1; Kotlin 2.1.20; API 36; NDK 28.2.13676358; CMake 3.31.6; Oboe 1.10.0; Android 15/API 35 emulator | Debug/Release/test dual-ABI APKs and lint pass; packaged UI-to-AAudio chain renders at 48 kHz; injected transient focus loss stops the runtime and focus gain reopens AAudio with finite callbacks; foreground/background/screen-off/idle lifecycle passes. Physical device, real competing-app focus, and route acceptance remain unverified |
| iOS arm64 | implementation-complete; production lifecycle/key ownership simulated | 2026-09-03 | MSVC 19.51, GNU 15.2.0, Emscripten 6.0.5, and Clang ASan/UBSan policy/key tests; Apple SDK unavailable | Complete UIKit/WKWebView application, strict native bridge, RemoteIO/AVAudioSession lifecycle, foreground hardware keys, legal background policy, private sequence storage, privacy manifest, app icon, and simulator/device Xcode pipeline are present. The exact controller-consumed policy and hardware-key ownership state machines execute across three main toolchains; all 30 note mappings, Space sustain, repeat/rollback/release semantics, and sanitizer boundaries pass. A fail-closed CI runner is wired to install and launch the app and verify the packaged UI plus valid/rejected bridge requests on an iPhone Simulator. That runner has not executed here; Apple framework compilation, UIKit delivery, OS lifecycle, audio, and device acceptance remain unverified |
| HarmonyOS arm64/x86_64 | formal product implementation-complete/source-checked; policy/native boundary simulated; OpenHarmony compatibility build-verified | 2026-09-03 | OpenHarmony 5.0.0.71/API 12; Hvigor/OHOS plugin 5.8.9; JDK 21.0.12.1; Node.js 22.16.0; MSVC 19.51, GNU 15.2.0, Emscripten 6.0.5, Clang ASan/UBSan, and AArch64 QEMU simulation; no DevEco/device | Clean Debug and Release compatibility HAPs contain ArkTS bytecode plus AArch64 and x86-64 OHAudio libraries; Release is unsigned and 2,953,954 bytes. The recorded clean-run archive SHA-256 is `3AE2368D3A2F1901390B6C56164A26DE8A0AC07700DD512F84FBEE6664FAE312`; stable packaged-bytecode SHA-256 is `C5DB99A0968F9514BC37BEEB6731E1E4BCF12BB3E14C640BB647C945077C545B`, while the raw archive includes timestamps. The exact production `.ets` policy source executes without transformation under Node and compiles into both HAP variants. The unchanged production Node-API bridge and OHAudio host execute registration, strict validation, status/event/recording transfer, fast/fallback PCM, route/interruption/error recovery, and failure injection across x64, Wasm, sanitizers, and AArch64 QEMU. AudioSession, AVSession, continuous-task delivery, HarmonyOS scheduling, signing, and device acceptance remain unverified |
| ESP32 | build-verified; emulated-firmware (M9 implementation) | 2026-09-03 | ESP-IDF 6.1; GNU 15.2.0; Espressif QEMU 9.2.2; no board attached | Default 1,018,256-byte and Web 1,551,168-byte images build; isolated firmware boots in QEMU and passes storage, shared sequence/C4, production input/control, and FreeRTOS audio with a virtual sink; GPIO, BLE/Classic HID, I2S, A2DP Source/AVRCP, NVS/FAT, recovery gestures, control task, and physical AP-only configuration are implemented; physical radio/audio/real-time/long-run acceptance is unverified |
| ESP32-S3 | build-verified; emulated-firmware (M9 implementation) | 2026-09-03 | ESP-IDF 6.1; GNU 15.2.0; Espressif QEMU 9.2.2; no board attached | Default 796,832-byte and Web 1,302,192-byte images build; isolated firmware boots in QEMU and passes storage, shared sequence/C4, production input/control, and FreeRTOS audio with a virtual sink; GPIO, BLE and USB HID, I2S, NVS/FAT, recovery gestures, control task, and physical AP-only configuration are implemented; Classic A2DP is explicitly absent; physical input/audio/real-time/long-run acceptance is unverified |

## Capability boundaries

- Desktop and mobile Bluetooth audio is routed by the operating system.
- Bluetooth latency does not use the wired low-latency acceptance threshold.
- Mobile hardware keyboard input is promised only while the application can
  legally receive it.
- Browser keyboard input requires focus and browser audio requires a user gesture.
- ESP32-S3 does not advertise Classic Bluetooth A2DP Source capability.
