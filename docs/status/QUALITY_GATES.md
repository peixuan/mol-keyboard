# Quality Gates

| Gate | State | Evidence |
|---|---|---|
| M0 engineering baseline | passed | Windows x64 and Wasm Debug/Release tests pass; test-enabled configuration fails closed if Node.js cannot register the exact HarmonyOS production-policy test or Python cannot register the evidence-parser/process-simulation tests; isolated installed SDK gates compile every public header in C11/C++17 and run C/C++ consumers; ESP32 and ESP32-S3 firmware compile the same ISO C11 core with ESP-IDF 6.1 |
| M1 portable sound path | passed | Same C4 sequence is 261.25 Hz with finite output in Native, Wasm, and the ESP32 32 kHz core path; allocation-free desktop/worklet/I2S callbacks and Android Oboe, Apple AudioUnit, and Harmony OHAudio call entries exist |
| M2 music semantics | passed | All required transforms, gesture ownership, sustain, portamento, transport, metronome, and six arpeggiator modes pass 17 Native and 15 Wasm tests; both targets match the 35-event golden digest; exact transport math reaches the two-hour frame with zero drift |
| M3 instruments and DSP | passed | All DSP primitives, 18 compiled procedural patches, stereo effects/master safety, calibrated audio golden, Native/Wasm metric conformance, sanitizer fuzzing, Tiny/feature-off builds, and current ESP32/ESP32-S3 map evidence pass |
| M4 recording and tools | passed | Bounded streaming Mol Sequence v1, deterministic processed-event recording/playback, JSON/MIDI/editing tools, three-format offline WAV renderer, corruption/round-trip tests, parser/writer fuzzing, and one shared Native/Wasm/ESP32 sequence fixture pass |
| M5 desktop product | automated acceptance passed; physical I/O pending | The wxWidgets production shell embeds the shared Web UI through WebView2, WebKitGTK, or WKWebView, while the optional fully native debugger controls the independent daemon over local IPC. GUI-enabled Release suites pass on Windows and Linux; real WebView2 and WebKitGTK/Xvfb capability gates pass. Fresh macOS 15 arm64 and Intel x86_64 jobs each pass 96/96 native tests, including the real LaunchAgent lifecycle, then build both GUIs, pass 4/4 WKWebView/native-debugger tests, and audit the extracted package plus independent no-UI daemon/CLI lifecycle. Native Windows ARM64 passes 99/99 and Linux ARM64 passes 101/101 on matching hosted architectures; the cross-build and AArch64 QEMU gates remain independent coverage. Physical Bluetooth, keyboard, MIDI, audio routes, hotplug, suspend/resume, scheduling, and latency remain explicit host/device gates |
| M6 Web/PWA | passed | Production PWA, standalone and authenticated service modes, fail-closed real-daemon browser acceptance, current Windows/Linux Chrome-to-daemon runs, Chrome/Edge realtime AudioWorklet automation, Firefox offline AudioWorklet/Wasm DSP, mobile-layout automation, offline audio startup/play, MessagePort fallback, and SAB path pass. The complete Linux browser workflow passes. A fresh hosted macOS 15 arm64 gate directly drives actual Safari 26.6.1 with SafariDriver and requires the complete production UI, realtime Wasm/AudioWorklet readiness, MessagePort transport, and a keyboard event reaching the engine with zero drops |
| M7 Android and iOS | implementation complete; physical acceptance pending | Exact candidate `95c6cee` passes dual-ABI Debug/Release/test/lint builds and a fail-closed Android 15/API 35 headless-emulator gate. The packaged x86_64 app is runtime-verified through AAudio at 48 kHz, all 30 production hardware-key mappings, repeat suppression, injected transient focus loss/gain, foreground notification, background, screen-off, idle shutdown, and service cleanup. The same candidate passes warnings-as-errors iOS x86_64/arm64 Simulator and arm64 Device builds with Xcode 16.4, validates the complete bundle, installs and launches it in an iPhone Simulator, and verifies the production UI plus valid/rejected reply-bridge requests. Portable iOS lifecycle/key-state and sanitizer tests also pass. Physical mobile audio, routes, interruptions, hardware input, background endurance, signing, and latency remain explicit gates |
| M8 HarmonyOS | implementation complete; policy/native boundary simulated; OpenHarmony compatibility build passed; formal platform acceptance pending | Complete Stage/ArkUI application, strict API 12 Node-API/OHAudio path, actual low-latency status and fallback, AudioSession/AVSession/official continuous-task lifecycle, private storage, and audited HAP pipeline are present. The exact production background-policy `.ets` source executes without transformation under Node and compiles into both HAP variants. The unchanged production Node-API bridge and OHAudio host pass controlled registration, validation, status/event/recording transfer, fast/fallback PCM, route/interruption/error recovery, ASan/UBSan, and AArch64 QEMU execution. Official public OpenHarmony API 12 Debug/Release HAPs build after an explicit clean, contain ArkTS bytecode and both required native ABIs, and expose a stable bytecode content digest; AudioSession/AVSession/continuous-task delivery, formal DevEco build/signing, and physical-device acceptance remain explicit gates |
| M9 ESP32 product | implementation complete; emulated firmware passed; hardware acceptance pending | Clean exact-source candidate `19735a9` builds all four default/Web ESP32-family images with GPIO, BLE/Classic/USB HID capability split, NVS/FAT persistence, physical recovery, isolated control, original-ESP32 A2DP Source, AP-only Web configuration, map budgets, and a fail-closed 30-minute HIL runner. Both real ESP-IDF images boot in Espressif QEMU and exercise storage, conformance, 12 production commands, and at least 96,000 FreeRTOS audio frames with a virtual sink; 30-minute virtual telemetry and injected-failure rejection also pass. Physical peripherals, real-time behavior, and board endurance remain unverified |
| M10 v1.0.0 release | automated gates passed; external acceptance blocked | Native/Wasm/Web/Android/iOS regression, Android and iPhone Simulator acceptance, Windows/Linux/macOS system-WebView and native-debugger regression, native Windows/Linux ARM64, Linux AArch64 QEMU, both ESP32-family firmware QEMU smokes, virtual HIL fault injection, Release+LTO profiles, shared/installed SDK and ABI checks, coverage, static analysis, sanitizers/fuzzers/TSan, endurance, size, dependency/license/SBOM, audited desktop packages with both GUI lifecycles plus an independent headless lifecycle, and clean-checkout reproductions pass. The physical latency analyzer is validated, but formal HarmonyOS and physical device/peripheral/route/endurance/latency gates prevent a truthful `v1.0.0` tag |

The status `build-verified`, `runtime-verified`, or `device-verified` is used only
after the corresponding real check succeeds.
